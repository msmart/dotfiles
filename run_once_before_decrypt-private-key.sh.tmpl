#!/bin/sh

if [ ! -f "/home/msm/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/msm/.config/chezmoi"
    chezmoi age decrypt --output "/home/msm/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/msm/.config/chezmoi/key.txt"
fi
